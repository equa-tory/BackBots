using System.Collections;
using System.Collections.Generic;
using UnityEngine.AI;
using UnityEngine;
using UnityEditor;

[CustomEditor(typeof(unit_component_nav))]
public class MoveToClosestTargetEditor : Editor
{
    // private void OnSceneGUI() {
    //     unit_component_nav moveToClosestTargetEditor = (unit_component_nav)target;
        
    //     if(!moveToClosestTargetEditor) return;

    //     int closestIndex = 0;
    //     float closestTargetDistance = float.MaxValue;

    //     path = new NavMeshPath();
    //     NavMeshPath path = paths[i];

    //     if(NavMesh.CalculatePath(moveToClosestTargetEditor.transform))
    // }
}
